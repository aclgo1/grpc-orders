version: "3.9"

services:
    app:
      build: .
      restart: always
      ports:
        - 50055:50055

    # db:
    #   image: postgres:15.2-alpine3.17
    #   container_name: db
    #   environment:
    #     POSTGRES_PASSWORD: grpc-orders
    #     POSTGRES_USER: grpc-orders
    #     POSTGRES_DB: grpc-orders      
    #   ports:
    #     - 5432:5432
    #   healthcheck:
    #     test: ["CMD-SHELL", "pg_isready -U grpc-orders"]
    #     interval: 3s
    #     timeout: 5s
    #     retries: 5